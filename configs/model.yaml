model:
  name: deeplabv3plus

encoder:
  backbone: resnet50
  pretrained: true

input:
  channels: 12

output:
  classes: 6

training:
  loss: dice_focal
  optimizer: adamw
  scheduler: cosine

checkpoint:
  save_best_only: true
  monitor: val_miou